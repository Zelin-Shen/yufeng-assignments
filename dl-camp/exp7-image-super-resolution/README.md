# Exp 7: Image Super-Resolution

Single-image super-resolution with a GAN-based SR network.

- Original assignment: 图像超分辨
- Final iteration (`try6`) improvements: asymmetric learning-rate control, spectral-normalization reconstruction, forced-update mechanism, adaptive intervention
- Artifacts: model/training code (`*.py`) and notebooks (`*.ipynb`)
