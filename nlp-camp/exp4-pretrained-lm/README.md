# NLP Exp 4: Pretrained Language Models for Relation Extraction

Applying pretrained language models to relation extraction over Wikidata relations.

- Key techniques: BERT / RoBERTa fine-tuning, low-resource regimes at 10% / 50% / 100% training data
- Artifacts: `Project/` (training notebooks, `dataset/rel2id.json` + `rel_info.json`, `results/*_results.json`, `all_experiments.json`)
